.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install micasa

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv micasa
    $ pip install micasa
